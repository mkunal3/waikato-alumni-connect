
  # alumni-connect (Community)

  This is a code bundle for alumni-connect (Community). The original project is available at https://www.figma.com/design/316ML1jbWykzdUVZEIZeZi/alumni-connect--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  