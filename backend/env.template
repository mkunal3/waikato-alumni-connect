# Database
DATABASE_URL="postgresql://user:password@localhost:5432/waikato_connect"

# JWT Secret (generate a random string: openssl rand -hex 32)
JWT_SECRET="your-secret-key-here-minimum-32-characters"

# Environment
NODE_ENV="production"

# Frontend URL (update after frontend deployment)
FRONTEND_URL="http://localhost:3000"

# Port (usually set by platform, but can override)
PORT=4000

# Email Service (Optional - for production email sending)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-api-key
# SMTP_FROM=noreply@waikato-connect.ac.nz
